## v0.6.3 — Stage 5–6: core/HAC + micro/panel/DiD correctness (#151–#189, 39 issues)

Closes the full Stage 5–6 cluster of the remediation backlog (tracking: #373). All 39 issues land TDD-style, one commit per issue (grouped where the specs mandate coherent landing), each verified against analytic/property oracles or external references.

### Highlights
- **Core/HAC (#151–#162):** kernel-specific Andrews (1991) bandwidth constants, QS kernel sum, Andrews-Monahan prewhitening, system-HAC cross-equation blocks, robust_inv/safe_cholesky hardening, Kalman Joseph form, narrowed draw-rejection catches, Kilian bootstrap relabel, FEVD orthogonality guard, core low-severity batch, perf.
- **DiD covariance (#164–#169):** CS influence-function inference, SA joint saturated regression, BJS Prop-6 IF variance (+ full cross-horizon covariance `att_vcov`), covariance-aware pretrend Wald, Goodman-Bacon timing-window weights.
- **#163 Honest DiD (CRITICAL):** faithful Rambachan-Roth (2023) port replacing the naive `Mbar·(e+1)` bound — Δ^SD(M) Armstrong-Kolesár FLCI (matches R `HonestDiD::findOptimalFLCI` to ≤3e-4) and Δ^RM(M̄) closed-form identified set (matches R's LP union exactly) + delta-method CI (documented interim for the ARP conditional test). R oracle values pinned in-tree.
- **Micro/panel (#170–#189):** ordered-model observed-information Hessian, partial first-stage F + Cragg-Donald/Kleibergen-Paap/Stock-Yogo, SMM optimal weighting from influence contributions, GARCH-family Bollerslev-Wooldridge sandwich SEs (+ covariance cache), WLS vcov, robust Hansen J (+ NaN p-value under identity weighting), mlogit null LL, MacKinnon (1996) ADF/PP p-values, **Doornik (1998) Johansen p-values** (gretl-transcribed, MHM-validated), Ng-Perron fixed-sample ADF lag selection, PVAR instrument/covariance/innovation fixes, AB one-step H-matrix weighting, dynamic-panel GMM covariance + AR/Hansen diagnostics, generalized-inverse Hausman, panel logit rewrites (log-space DP, adaptive GH), **Bai-Perron q- and trimming-indexed critical values** (mbreaks-transcribed), low-severity batch (M-27..M-36), performance batch (bit-identical outputs).

### Formerly blocked items — resolved with in-env oracles this stage
- #163: R 4.5 + HonestDiD 0.2.8 installed (toolchain fixes documented in the commit); exact R pins committed.
- #177 (Johansen half): Doornik gamma-approximation coefficients transcribed programmatically from gretl (GPL); case mapping pinned empirically against statsmodels' MHM tables.
- #185: Bai-Perron 2003 tables transcribed programmatically from the `mbreaks` R package (no hand-typed digits).

### Verification
Per-issue relevant test files run locally with `MACRO_MULTIPROCESS_TESTS=1` (never the full suite, per policy); docs examples verified for every touched page (`binary_choice.md`, `did.md`, `event_study.md`, `tests_breaks.md`, `panel_reg.md`, `volatility.md`, `data.md`, `regression.md` — all OK). Plots regenerated (plotting/reg.jl + plotting/did.jl touched). Project.toml → 0.6.3; README updated.

Full suite left to CI. The docs `build` job is expected red-by-design until Stage 12 (#276+).
