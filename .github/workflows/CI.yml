name: CI

on:
  push:
    branches:
      - main
    tags: ['*']
    paths-ignore: ['docs/**', '**.md', '.claude/**']
  pull_request:
    paths-ignore: ['docs/**', '**.md', '.claude/**']
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ startsWith(github.ref, 'refs/pull/') }}

jobs:
  test:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    permissions:
      actions: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        version:
          - 'lts'    # Long-term support (1.10)
        os:
          - ubuntu-latest
          - windows-latest
        arch:
          - x64
        include:
          # Full-fidelity suite: LTS on macos arm64 (FAST=0, all groups).
          - version: 'lts'
            os: macos-latest
            arch: arm64
          # Numerical suite + Optim v1 dual-major compat (T153/#252): Julia 1.10
          # (== lts) as a distinct matrix string so it pins Optim@1 and runs
          # MACRO_NUMERICAL_CI (solver / Euler / IRF / published-number tests only).
          - version: '1.10'
            os: ubuntu-latest
            arch: x64
            optim_v1: true

    steps:
      - uses: actions/checkout@v7

      - uses: julia-actions/setup-julia@v3
        with:
          version: ${{ matrix.version }}
          arch: ${{ matrix.arch }}

      - uses: julia-actions/cache@v3
        with:
          # Windows depot artifacts are ~1.5 GB (3× Linux) and dominate the
          # 4-minute cache upload. compiled/ is what Pkg.test reuse needs.
          cache-artifacts: ${{ matrix.os != 'windows-latest' }}

      # Optim v1 dual-major compat cell (T153/#252): pin Optim to the v1 major before
      # instantiate/precompile so the suite runs against Optim v1 (default cells resolve v2).
      - name: Pin Optim to the v1 major
        if: matrix.optim_v1
        run: julia --project=. -e 'using Pkg; Pkg.add(Pkg.PackageSpec(name="Optim", version="1"))'

      - uses: julia-actions/julia-buildpkg@v1

      # Cell roles:
      #   macos LTS     — full suite (FAST=0, all groups, bounds checks)
      #   ubuntu 1.10   — numerical suite (MACRO_NUMERICAL_CI=1) + Optim v1 pin
      #   ubuntu LTS    — linux full suite (FAST=0); no coverage post-process
      #   windows LTS   — reduced-sampling smoke (FAST=1, threaded single process)
      #
      # Windows uses the threaded single-process runner (package loaded once).
      # Multiprocess on NTFS pays ~6 min per child to load compiled modules; 17
      # children made that the job. HA-DSGE still gets 2 OpenBLAS threads via
      # the runner; every other group stays at 1.
      - uses: julia-actions/julia-runtest@v1
        env:
          JULIA_NUM_THREADS: ${{ matrix.os == 'windows-latest' && 'auto' || '1' }}
          OPENBLAS_NUM_THREADS: "1"
          MACRO_MULTIPROCESS_TESTS: ${{ matrix.os == 'windows-latest' && '0' || '1' }}
          MACRO_FAST_TESTS: ${{ matrix.os == 'windows-latest' && '1' || '0' }}
          MACRO_NUMERICAL_CI: ${{ matrix.os == 'ubuntu-latest' && matrix.version == '1.10' && '1' || '0' }}
          MACRO_CHECK_BOUNDS: ${{ matrix.os != 'windows-latest' && '1' || '0' }}

  # RNG-everywhere lint (#243): every drawing function must thread an rng; no bare
  # rand/randn on the global RNG in src/. Dependency-free (no Julia), so it runs fast.
  rng-lint:
    name: RNG-everywhere lint (#243)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Ban bare rand/randn on the global RNG in src/
        run: |
          # Docstring-aware: skips content inside """...""" blocks and # comment lines, so
          # docstring `# Examples` (randn(n), randn(200,3), rand(1:k), …) are not flagged.
          # Whitelist on real-code lines: rng-threaded draws whose first argument is named
          # `rng`, `<prefix>_rng`, or `rng_<suffix>` (rand(rng,…)/randn(local_rng,…)/
          # randn(rng_fit,…)); MersenneTwister/Random.default_rng seeds; and an explicit
          # `# rng-lint: ignore` opt-out. Everything else drawing bare on the global RNG fails.
          viol=$(awk '
            FNR==1 { indoc=0 }
            {
              tmp=$0; c=gsub(/"""/,"",tmp); startin=indoc
              k=c; while (k-- > 0) indoc=!indoc
              if (startin || (c % 2 == 1)) next
              if ($0 ~ /^[[:space:]]*#/) next
              if ($0 !~ /(^|[^A-Za-z_.])(rand|randn|randn!)\(/) next
              line=$0
              if (line ~ /\(([A-Za-z_][A-Za-z_0-9]*_)?rng(_[A-Za-z_0-9]+)?[,)]/) next
              if (line ~ /(MersenneTwister|Random\.default_rng|rand\(UInt)/) next
              if (line ~ /# *rng-lint: *ignore/) next
              printf("%s:%d:%s\n", FILENAME, FNR, $0)
            }
          ' $(find src -name "*.jl" | sort))
          if [ -n "$viol" ]; then
            echo "::error::Bare rand/randn drawing on the global RNG (see #243 rng-everywhere policy):"
            echo "$viol"
            exit 1
          fi
          echo "rng-lint OK — no bare rand/randn in src/"
