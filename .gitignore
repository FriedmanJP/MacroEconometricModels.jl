# Local plans (not for remote)
docs/plans/
docs/superpowers/

# Local scripts (not for remote)
scripts/

# Benchmarks (local only)
benchmarks/
test/oracle/_out/
test/oracle/_data/

# Coverage artifacts (never commit — produced by julia --code-coverage)
*.cov
*.jl.*.cov
lcov.info
